require('dotenv').config({ path: '.env.local' }); const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); supabase.auth.admin.createUser({ email: 'test_12345@test.com', password: 'TestPassword123!', email_confirm: true }).then(console.log).catch(console.error);